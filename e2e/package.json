{
  "name": "eramix-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Pi-only Playwright E2E suite — see README.md. Deliberately outside the pnpm workspace (pnpm-workspace.yaml only globs apps/* and packages/*), so `pnpm install`/`pnpm run check` at the repo root never touches this package and the laptop never installs a browser stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@axe-core/playwright": "^4.10.0"
  }
}
