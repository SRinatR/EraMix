{
  "name": "@eramix/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/main.js",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "eslint . -c ../../eslint.config.js --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "@eramix/application": "workspace:*",
    "@eramix/domain": "workspace:*",
    "@eramix/infrastructure": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
